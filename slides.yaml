# app example

App Object            (&) <- user
  \_ HPA        0     (+)
  \_ PDB        0     (+)
  \_Deployment Object (+)
    \_ ReplicaSet 0   (*)
    \_ ReplicaSet 1   (*)
    \_ ReplicaSet 2   (*)
        \_ Pod0       (!)
        \_ Pod1       (!)
        \_ Pod3       (!)

 
(&) Managed by the User 
(+) Managed by the App Controller
(*) Managed by the Deployment Controller
(!) Managed by tge ReplicaSet Controller
