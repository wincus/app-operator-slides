App Object            (&) <- user
  \_Deployment Object (+)
    \_ ReplicaSet 0   (*)
    \_ ReplicaSet 1   (*)
    \_ ReplicaSet 2   (*)
        \_ Pod0       (!)
        \_ Pod1       (!)
        \_ Pod3       (!)
  \_S3 Object         (+)
    \_ConfigMap       (=)
    \_VaultSecret     (=)
      \_Secret        (.)      

(&) Managed by the User
(+) Managed by the App Controller
(*) Managed by the Deployment Controller
(!) Managed by the ReplicaSet Controller
(=) Managed by the S3 Controller
(.) Managed by the SecretController
