# deployment example

Deployment Object (&) <- user
  \_ ReplicaSet 0 (*)
  \_ ReplicaSet 1 (*)
  \_ ReplicaSet 2 (*)
     \_ Pod0      (!)
     \_ Pod1      (!)
     \_ Pod3      (!)

(&) Managed by the User 
(*) Managed by the Deployment Controller
(!) Managed by tge ReplicaSet Controller
